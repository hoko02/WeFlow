## 1. Operator Case Snapshot Contract

- [x] 1.1 Add the closed v1 `OperatorCaseSnapshot` JSON Schema and matching Python/TypeScript exports for snapshot, tenant, fixture, Case/revision/workflow, current-state, source-report/evidence/replay roots, counts, offline capabilities, and ordered typed timeline entries; verify one complete valid fixture is accepted and undeclared or arbitrary payload maps are rejected in both languages.
- [x] 1.2 Add semantic validation for canonical snapshot hashing, fixed fixture/state, tenant/Case/revision/workflow linkage, unique contiguous entry/source identities, phase and predecessor order, aggregate counts, source/root hashes, hard-gate precedence, and no-authority capability flags; verify focused tests reject every mismatched invariant.
- [x] 1.3 Add cross-language valid/invalid fixtures for foreign identity, detached source/root/hash, missing/duplicate/out-of-order entry, predecessor/count mismatch, stale approval or denial presented as success, raw/secret-like/executable fields, unsafe paths, caller authority, live provider/customer-success, and mutation/effect capability; verify all retained fixtures keep their prior result and record the new schema fingerprint only after parity passes.

## 2. Source-Backed Snapshot and Canonical Reader

- [x] 2.1 Add a public deterministic operator-case source boundary that runs the allowlisted API-503 happy path in an injected fresh SQLite store and returns only validated durable source records through existing public ledger/workflow/simulator APIs; verify it does not query private tables, read task-local expected mirrors, touch the default store, initialize a provider, or perform network/external writes.
- [x] 2.2 Build the bounded `OperatorCaseSnapshot` from the validated source bundle with the explicit intake-to-evidence phase order, source record IDs/hashes, transitions/observations, gate/recovery annotations, counts, roots, fixture-local final state, and canonical hash; verify two fresh runs are byte-equivalent and every timeline entry maps to exactly one actual source record without duplicate natural/idempotency keys.
- [x] 2.3 Add a read-only canonical report source that resolves only `reports/add-offline-operator-case-timeline-acceptance.json`, rejects duplicate JSON keys and unsafe/out-of-repository paths, revalidates the acceptance envelope and full snapshot, and supports explicit injection for tests; verify missing, malformed, unsafe, stale, detached, and partial reports emit no snapshot and never create or repair a store/report.

## 3. Tenant-Scoped Platform API

- [x] 3.1 Inject the operator snapshot reader behind a small optional read protocol while keeping Case/workflow/effect ownership unchanged; verify the API factory remains import-side-effect free, existing routes work when the reader is absent, and operator-report availability does not affect health, foundation capabilities, or evaluation-report availability.
- [x] 3.2 Implement only `GET /v1/operator/cases/api-503.v1`, derive tenant from `X-WeFlow-Synthetic-Actor`, reject query/body/path/version selectors, and map missing/foreign evidence to `operator_case_not_found` and integrity failures to `operator_case_not_ready`; verify the valid response satisfies the canonical snapshot/OpenAPI contract and exposes no source path, foreign identity, raw exception, or mutation authority.
- [x] 3.3 Add Platform API integration/security tests for authorized, unknown-actor, foreign-tenant, missing, malformed, duplicate-key, tampered, unsafe, stale, detached, arbitrary-selector, alternate-path/version, and unsupported-method requests; verify every request leaves the canonical report, default store, Cases, workflows, approvals, deliveries, effects, and evaluation report byte-unchanged.

## 4. Offline Operator Case Workspace

- [x] 4.1 Add the TypeScript fixed-route transport, complete runtime snapshot validation, and pure render-model mapping for loading, ready, not-found, identity-denied, and integrity-not-ready states; verify invalid or partial response content never reaches the view and health/evaluation loading remains independent.
- [x] 4.2 Add the Vue Case workspace showing fixture/Case/revision/workflow identity, source/snapshot/evidence roots, capability labels, counts, the full ordered phase timeline, and one selected source-linked detail with transition/observation, gate/recovery status, hashes, and reason code; verify `DELIVERY_RECORDED` is labeled fixture-local and no live send, customer receipt/resolution, completion, approval authority, or Replay control is rendered.
- [x] 4.3 Add deterministic console tests for the complete canonical timeline, selected entry, failed-gate/denial/stale-approval/restart/timeout precedence, five safe surface states, bounded local-only labels, and forbidden raw JSON/HTML/customer-success claims; verify lint, typecheck, tests, and the production Vite build pass without a new browser-test or network dependency.

## 5. Offline Acceptance and Documentation

- [x] 5.1 Add `python scripts/dev.py operator-case-timeline-acceptance` to execute two fresh source-backed baselines, validate the snapshot and fixed API/render paths, run the production console build, compare the default store and retained reports, and emit the canonical redacted report only after success; verify equal snapshots and zero default-store/source-report mutations, duplicate natural/idempotency keys, network/model/provider calls, external-write attempts, or unauthorized effects.
- [x] 5.2 Add the missing/foreign, malformed, duplicate-key, unsafe path/field, detached hash/predecessor, duplicate/out-of-order source, count mismatch, stale approval, policy denial, restart/timeout duplicate completion, unsupported customer-success/authority, arbitrary-selector, and unsupported-method acceptance matrix plus prior-report preservation; verify every negative path emits only its stable safe classification and no partial snapshot.
- [x] 5.3 Run repository check/lint/typecheck, Python and TypeScript contract parity, focused and aggregate tests, secret hygiene, operator acceptance, production build, and strict OpenSpec validation; update README, the Operator Case development guide, and `docs/PROJECT_MEMORY.md` with exact verified metrics, report paths, limitations, and the next-stage Replay-control gate without claiming live, customer-verified, or complete business-workflow capability.
