[project]
name = "weflow-telemetry"
version = "0.1.0"
description = "Redacted local telemetry utilities for WeFlow"
requires-python = ">=3.12,<3.15"
dependencies = []

[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"


[tool.hatch.build.targets.wheel]
packages = ["src/weflow_telemetry"]
