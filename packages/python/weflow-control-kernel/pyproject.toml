[project]
name = "weflow-control-kernel"
version = "0.1.0"
description = "Deterministic control-kernel interfaces for WeFlow"
requires-python = ">=3.12,<3.15"
dependencies = [
  "weflow-contracts",
]

[tool.uv.sources]
weflow-contracts = { workspace = true }

[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"


[tool.hatch.build.targets.wheel]
packages = ["src/weflow_control_kernel"]
